/**
 * STUB temporal — el hook real está siendo construido en otro chat.
 *
 * `useTituloPestana(titulo)` está pensado para actualizar el `document.title`
 * según el contenido de la pestaña activa (nombre de la nota, contacto,
 * tipo de actividad, etc). Mientras esa implementación no se commitee,
 * este stub no hace nada — la pestaña queda con el título por defecto
 * del layout. Cuando el chat correspondiente push'ee la versión real,
 * sobreescribe este archivo y todo empieza a funcionar.
 */
export function useTituloPestana(_titulo: string | null | undefined): void {
  // no-op
}
