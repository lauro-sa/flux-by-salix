'use client'

/**
 * STUB temporal — el selector real está en construcción en otro chat.
 *
 * `SelectorVisitaPresupuesto` permite vincular una visita previa al
 * presupuesto (para reutilizar dirección, contacto y notas técnicas).
 * Mientras no se commitee la versión real, el selector no aparece en
 * `SeccionCliente` — el resto del editor de presupuesto funciona normal.
 */

interface Props {
  [k: string]: unknown
}

export default function SelectorVisitaPresupuesto(_props: Props) {
  return null
}
