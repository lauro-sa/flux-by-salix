'use client'

/**
 * STUB temporal — la sección real está en construcción en otro chat.
 *
 * `SeccionRelevamientoOT` muestra el formulario/vista del relevamiento
 * técnico inicial de una orden de trabajo. Mientras no se commitee la
 * implementación real, la sección no aparece dentro de `VistaOrdenTrabajo`
 * — el resto de la orden funciona normal.
 */

interface Props {
  [k: string]: unknown
}

export default function SeccionRelevamientoOT(_props: Props) {
  return null
}
