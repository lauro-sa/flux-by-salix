'use client'

/**
 * STUB temporal — la sección real está en construcción en otro chat.
 *
 * `SeccionBitacoraOT` muestra el feed cronológico de eventos de la
 * orden de trabajo (cambios de estado, tareas completadas, comentarios).
 * Mientras no se commitee la versión real, no aparece dentro de
 * `VistaOrdenTrabajo` — el resto de la orden funciona normal.
 */

interface Props {
  [k: string]: unknown
}

export default function SeccionBitacoraOT(_props: Props) {
  return null
}
