import { Cargador } from '@/componentes/ui/Cargador'

/** Loading global de Flux — se muestra al navegar entre secciones */
export default function Loading() {
  return <Cargador tamano="pagina" />
}
