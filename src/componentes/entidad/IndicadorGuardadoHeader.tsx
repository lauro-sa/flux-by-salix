'use client'

/**
 * STUB temporal — el componente real está en construcción en otro chat
 * (junto con `useIndicadorGuardado`).
 *
 * Va al lado del breadcrumb en el Header. Mostrará el estado de
 * guardado global (guardando / guardado / error). Por ahora no
 * renderiza nada — el Header queda sin indicador hasta que la versión
 * real se commitee.
 */
export function IndicadorGuardadoHeader() {
  return null
}

export default IndicadorGuardadoHeader
