'use client'

/**
 * STUB temporal — el componente real está en construcción en otro chat.
 *
 * Va dentro del modal de "Enviar documento" para avisar al usuario
 * cuando los recursos (PDF, link público) se están regenerando porque
 * el presupuesto cambió. Hasta que la versión real se commitee, no
 * renderiza nada — el modal funciona pero sin el aviso de sync.
 */

interface Props {
  [k: string]: unknown
}

export function BannerSincronizacion(_props: Props) {
  return null
}

export default BannerSincronizacion
