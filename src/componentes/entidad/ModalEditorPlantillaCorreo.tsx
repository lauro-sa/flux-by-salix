'use client'

/**
 * ModalEditorPlantillaCorreo — Re-exportacion desde sub-carpeta refactorizada.
 * La implementacion completa esta en _editor_plantilla/.
 * Se mantiene este archivo para no romper imports existentes.
 */

export { ModalEditorPlantillaCorreo } from './_editor_plantilla'
