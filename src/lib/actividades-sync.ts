/**
 * STUB temporal — la implementación real está en construcción en otro chat.
 *
 * `recalcularFechaVencimientoDesdeBloques` recalcula la fecha de
 * vencimiento de una actividad a partir de los bloques de calendario
 * vinculados (para mantener actividades.fecha_vencimiento sincronizada
 * con el primer bloque del calendario asociado).
 *
 * Mientras no se commitee la versión real: no-op. La actividad queda
 * con la fecha de vencimiento manual que ya tenía.
 */
export async function recalcularFechaVencimientoDesdeBloques(
  _admin: unknown,
  _actividadId: string,
): Promise<void> {
  // no-op
}
